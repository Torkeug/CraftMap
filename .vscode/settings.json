{
    "VsCodeTaskButtons.tasks": [
        {
            "label": "$(tools) Build CraftMap",
            "task": "Build CraftMap.exe",
            "tooltip": "Build CraftMap.exe via PyInstaller"
        }
    ]
}
